It provides built-in validation for email formats.

## Syntax:
```html
<input type='email'>
```

## Attributes:
All attributes are the same as `type='text'` except the following:
- **`multiple`** : allows the field to accept multiple email addresses separated by commas. 
	- It is a [[boolean attribute]].
	- Each email is validated individually for correctness.